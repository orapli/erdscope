.claude/
mastodon/
*.html
!docs/*.html
!src/erdscope/*.html
__pycache__/
HANDOFF.md
COLLAB_*.md
# internal design/working docs (kept local, like HANDOFF.md above)
PRODUCT_REVIEW.md
REFACTOR_PLAN.md
DESIGN_ROADMAP.md
NOTES_PHASE1_SPEC.md
DBML_REVIEW_NOTES.md
BACKLOG.md
dist/
build/
*.egg-info/

# stray large tool-call dump (scratch, not source)
/large_tool_results/

# benchmarks/: generated schemas/output/results, never the scripts themselves
# (run_bench.py normally generates everything in a temp dir and cleans up
# after itself, but --keep/--work-dir can point it at benchmarks/ directly)
benchmarks/*.db
benchmarks/bench_*.json
